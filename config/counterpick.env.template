# Copy this file to .env on the target server, then set values for that server.
# .env is intentionally ignored by Git.

# Self-hosted runtime
SELF_HOST=1
SELF_HOST_DB_PATH=./data/counterpick.sqlite
COUNTERPICK_ASSET_BASE=
COUNTERPICK_ADMIN_EMAILS=alebaba4565@gmail.com

# Optional authorized build feed. It must use the JSON shape in README.md.
DOTA_BUILD_FEED_URL=

# Enable only while the written Dota2ProTracker permission is archived.
D2PT_PERMISSION_CONFIRMED=1

# Public origin used for canonical and social metadata.
NEXT_PUBLIC_SITE_URL=https://conterpick.online
